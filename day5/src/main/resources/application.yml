server:
  port: 8082

spring:
  application:
    name: movie-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/cineverse
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# Custom file storage properties
file:
  upload-dir: ./uploads
